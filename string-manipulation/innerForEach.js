function innerForEach(str) {
  const brokenDownStr = str.split(" ");
  if (brokenDownStr.length <= 1) {
    return "Input more string to make a longer sentence";
  }
  for(let breakDown of brokenDownStr){
    return breakDown = breakDown.split('').sort()
 }

}
